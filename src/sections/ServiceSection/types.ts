export interface ServiceCardData {
  title: string;
  subtitle: string;
  description: string;
  icon: string;
  buttonIcon: string;
  highlighted?: boolean;
}

export interface StatData {
  value: string;
  label: string;
}
